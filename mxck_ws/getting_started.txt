Passwort: mxck

# test camera (host)
$ realsense-viewer

# start ros docker (host)
$ sudo docker run -it --rm \
--mount type=bind,source=/home/mxck/mxck_ws/mxck_hw,target=/catkin_ws \
--privileged \
--net=host \
--name ros_melodic_docker \
mxck_ros_hw:latest bash

# launch hardware - VESC, LEDs, USS, IMU, LIDAR, CAMERA (docker)
$ source devel/setup.bash
$ roslaunch mxck_run mxck_run.launch

# additional docker terminal (host)
$ sudo docker ps
$ sudo docker exec -it <container_id> bash

# lights (docker)
# 1. entry (0) headlights front/rear off, (1) headlights front/rear on, (2) high beam
# 2. entry (0) turn signal light off, (1) turn signal left, (2) turn signal right, (3) hazard warning lights
# 3. entry (t) duration headlight flasher t in ms
# 4. entry (0) brake lights off, (1) brake lights on

$ rostopic pub /lights std_msgs/UInt16MultiArray "layout:
  dim:
  - label: ''
    size: 0
    stride: 0
  data_offset: 0
data: [1, 3, 0, 0]" 

# additional brake light / status led (docker)
# [R, G, B, dutycylce_on, dutycycle_off]
rostopic pub /brake_light std_msgs/UInt16MultiArray "layout:
  dim:
  - label: ''
    size: 0
    stride: 0
  data_offset: 0
data: [255, 0, 0, 600, 600] " 

# ultrasonic sensors (docker)
$ rostopic echo /uss_values

# imu (docker)
$ rostopic echo /imu


